function setup() {
  createCanvas(500, 500, WEBGL);
}



function draw() {
  
  
 
  background('#0000');
  rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
  rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
  
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
  
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
  
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
  
   rotateX(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(100, 5);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
   rotateY(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(200, 2);
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
   rotateY(frameCount * 0.001);
  rotateY(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
   rotateX(frameCount * 0.001);
  rotateX(frameCount * 0.001);
  torus(200, 2);
  
  
}